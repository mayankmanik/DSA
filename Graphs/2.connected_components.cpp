#include <bits/stdc++.h>
using namespace std;


int main()
{
    int n,m;
    cin>>n>>m;
    vector<int> vis(n+1,0);
    for(int i=1;i<n;i++)
    {
        if(!vis[i])
        {
            // traversal(i);
        }
    }
    return 0;
}